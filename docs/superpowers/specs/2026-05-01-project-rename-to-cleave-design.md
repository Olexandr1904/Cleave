# Project Rename: Cleave/Tot → Cleave

**Date:** 2026-05-01  
**Status:** Approved

## Summary

Rename the project from "Cleave" (Python package / brand) and "tot" (repo/directory) to **"Cleave"** across the entire codebase, documentation, configuration, deployment artifacts, and GitHub.

**Rationale:** "Cleave" keeps the harvest/cutting metaphor (cuts through tickets, cuts developers out of routine work), sounds like a character name ("Cleave opened your PR"), is clean for open-source branding, and has no political or medical baggage. "Tot" was a meaningless working directory name.

---

## Scope of Changes

### 1. Python Package (Critical)

| File | Change |
|---|---|
| `pyproject.toml` | `name = "cleave"` → `name = "cleave"` |
| `main.py` line 5 | docstring: `Cleave —` → `Cleave —` |
| `main.py` line 19 | `version("cleave")` → `version("cleave")` |
| `main.py` line 35 | `prog="cleave"` → `prog="cleave"` |
| `main.py` line 36 | description string: `Cleave —` → `Cleave —` |
| `main.py` line 76 | startup log: `Cleave v` → `Cleave v` |
| `main.py` line 157 | log filename: `cleave-daemon.log` → `cleave-daemon.log` |
| `main.py` line 211 | event message: `Cleave v` → `Cleave v` |
| `cleave.egg-info/` | Delete directory; regenerated by `pip install -e .` |

### 2. Deployment & Service (Critical)

| File | Change |
|---|---|
| `deploy/cleave.service` | Rename file to `deploy/cleave.service`; update description, `WorkingDirectory`, `ExecStart`, `EnvironmentFile`, log paths from `cleave-prod`/`/var/log/cleave` to `cleave-prod`/`/var/log/cleave` |
| `scripts/deploy.sh` | `PROD_DIR="/home/admin0/cleave-prod"` → `cleave-prod`; `SERVICE_NAME="cleave"` → `"cleave"`; `cp deploy/cleave.service` → `cleave.service`; log dir `/var/log/cleave` → `/var/log/cleave` |

### 3. Config Defaults (High)

| File | Change |
|---|---|
| `config/schemas.py` | Default log dir `/var/log/cleave` → `/var/log/cleave`; bot name `"Cleave Bot"` → `"Cleave Bot"`; bot email `cleave@pipeline.local` → `cleave@pipeline.local` |
| `config-live.example/global.yaml` | Workspace base `/data/cleave` → `/data/cleave`; log dir `/var/log/cleave` → `/var/log/cleave`; operator comment |
| `config-live.example/projects/example-project/repos/example-repo.yaml` | Bot name/email; PR description mentioning "Cleave pipeline" → "Cleave pipeline" |

### 4. Agent & Bot Identity (High)

| File | Change |
|---|---|
| `agents/project-setup-agent.md` | Identity string, `commit_author_name`, `commit_author_email` |
| `integrations/telegram/intent_parser.py` line 33 | System prompt: `Cleave,` → `Cleave,` |
| `integrations/telegram/handlers/status.py` line 36 | Button label: `"Cleave Status"` → `"Cleave Status"` |
| `orchestrator/pr_creation.py` | PR body: `Cleave pipeline` → `Cleave pipeline` |
| `orchestrator/ticket_prioritizer.py` | `bot_name` default: `"Cleave Pipeline"` → `"Cleave Pipeline"` |
| `orchestrator/tool_sandbox.py` | Tool description: `Cleave config directory` → `Cleave config directory` |

### 5. Claude Commands (High)

| File | Change |
|---|---|
| `.claude/commands/add-project.md` | Title and description: `Cleave Pipeline` → `Cleave Pipeline` |
| `.claude/commands/list-projects.md` | Title and description |
| `.claude/commands/remove-project.md` | Title and description |

### 6. Root Documentation (High)

| File | Change |
|---|---|
| `README.md` | Title `# Cleave` → `# Cleave`; all body occurrences; clone instruction `cd cleave` → `cd cleave` |
| `CONTRIBUTING.md` | Title and body |
| `CLAUDE.md` | Two occurrences of "Cleave" in pipeline agent section |

### 7. Module Docstrings (Medium)

Files with docstrings only (no runtime impact):
- `config/config_loader.py`
- `config/resource_registry.py`
- `dashboard/web.py`
- `dashboard/event_store.py`
- `dashboard/events.py`
- `orchestrator/constants.py`
- `integrations/config/config_tools.py`
- `workspace/workspace.py` (also a generated string written to workspace .gitignore)

### 8. Documentation Files (Medium — mechanical)

Global find/replace across all `docs/**/*.md`:
- `Cleave` → `Cleave`
- `cleave` → `cleave`
- `/var/log/cleave` → `/var/log/cleave`
- `/data/cleave` → `/data/cleave`
- `cleave-prod` → `cleave-prod`
- `cleave.service` → `cleave.service`
- `cleave@pipeline.local` → `cleave@pipeline.local`

Affected files include all docs in: `docs/`, `agents/README.md`, `deploy/README.md`.

### 9. Test Files (Low)

| File | Change |
|---|---|
| `tests/unit/test_orchestrator_squash.py` | Bot name/email in test data |
| `tests/unit/test_ensure_branch_has_commits.py` | Bot name/email in test data |
| `tests/unit/test_workspace_manager.py` | `tmp_path / "cleave"` → `"cleave"` (3 occurrences) |
| `tests/unit/test_config_loader.py` | Assertion: `/var/log/cleave` → `/var/log/cleave` |
| `tests/unit/test_main.py` | Assertions checking `"Cleave v"` → `"Cleave v"` |
| `tests/unit/test_gradle_remediation.py` | Error message fixture: `/data/cleave/` → `/data/cleave/` |
| `tests/e2e/test_deferred_recovery.py` | `tmp_path / "cleave"` → `"cleave"` (3 occurrences) |
| `tests/e2e/conftest.py` | `tmp_path / "cleave"` → `"cleave"` (2 occurrences) |

### 10. Settings File (Low)

| File | Change |
|---|---|
| `.claude/settings.local.json` | `/data/cleave/` path references → `/data/cleave/`; `cleave` service name references |

### 11. GitHub & Git Remote (Manual step)

- Rename GitHub repo from `tot` to `cleave` via GitHub web UI (Settings → Repository name)
- After rename: `git remote set-url origin https://github.com/Olexandr1904/cleave.git`

---

## Replacements Reference

All string substitutions, in application order (longest/most-specific first):

| Old | New |
|---|---|
| `cleave@pipeline.local` | `cleave@pipeline.local` |
| `cleave-daemon.log` | `cleave-daemon.log` |
| `/var/log/cleave` | `/var/log/cleave` |
| `/data/cleave` | `/data/cleave` |
| `cleave-prod` | `cleave-prod` |
| `cleave.service` | `cleave.service` |
| `Cleave Bot` | `Cleave Bot` |
| `Cleave Pipeline` | `Cleave Pipeline` |
| `Cleave pipeline` | `Cleave pipeline` |
| `Cleave —` | `Cleave —` |
| `Cleave,` | `Cleave,` |
| `Cleave` | `Cleave` |
| `version("cleave")` | `version("cleave")` |
| `prog="cleave"` | `prog="cleave"` |
| `name = "cleave"` | `name = "cleave"` |
| `cleave` | `cleave` |

---

## Execution Approach

Use `sed -i` for mechanical find/replace across file groups. Then handle special cases (file renames, git remote) manually.

Order:
1. Python package files (pyproject.toml, main.py)
2. Deploy artifacts (service file rename + content, deploy.sh)
3. Config defaults + examples
4. Agent files + claude commands
5. Root docs (README, CONTRIBUTING, CLAUDE.md)
6. All other docs (bulk sed)
7. Python source files (orchestrator, dashboard, integrations, config, workspace)
8. Test files
9. .claude/settings.local.json
10. Delete cleave.egg-info/, run `pip install -e .`
11. GitHub repo rename + git remote update

---

## Out of Scope

- Renaming the local directory `/home/admin0/tot` — this is a filesystem path. The user may want to do this manually. The Python package name and brand are what matter.
- Updating any live/deployed production environment — this spec covers the source repo only.
